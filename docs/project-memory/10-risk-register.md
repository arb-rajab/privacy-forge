# Risk Register
> Purpose: known risks, owned and reviewed rather than forgotten
> Project: privacy-forge (public)
> Last updated: 2026-08-19 (Session 28 — R-01's concurrency and credential-scoping gaps closed for real, on top of Session 27's grant-enforcement proof)

| ID | Risk | Category | Impact | Likelihood | Mitigation | Status | Review date |
|---|---|---|---|---|---|---|---|

## Accepted residual risks

| ID | Risk | Category | Impact | Likelihood | Standing mitigation | Status | Accepted |
|---|---|---|---|---|---|---|---|
| R-08 | The Pest Browser Testing suite (`composer test:e2e`, `tests/Browser/`) hung in Session 16 on a Linux sandbox host, and was assessed there as "environment-specific, not a product regression" pending a check on a different host. That check happened at Session 17, on a genuinely different host (Windows 11 + Docker Desktop/WSL2) — and the hang reproduced with the identical signature, rejecting the "sandbox-specific" hypothesis. **Session 18 made one genuine, targeted attempt at the root cause and the hang did not resolve.** First, Session 17's own proposed mechanism was re-examined by actually reading the relevant source (`vendor/pestphp/pest-plugin-browser/src/Playwright/Servers/PlaywrightNpmServer.php`, `ServerManager.php`), not just re-asserted: the `sh -c` shell Session 17 flagged wraps the PHP→Node launch of `playwright run-server` — it does **not** sit between Node and the Chromium process it subsequently spawns. The actual CDP pipe (fds 3/4) is set up by Node's own direct, shell-free `child_process.spawn()` call when Playwright's already-running Node process launches Chromium — a hop the outer `sh -c` has no way to affect. **This narrows, and partly corrects, Session 17's mechanism theory**: the specific "PHP→Node `sh -c` breaks fd inheritance" story doesn't survive reading the code, even though the *symptom* it was proposed to explain is still real. Second, Session 18 searched Playwright-core's own bundled source (`node_modules/playwright-core/lib/coreBundle.js`) for a documented pipe-vs-WebSocket toggle for a *locally launched* browser — there isn't one exposed via any public Playwright or pest-plugin-browser API; WebSocket-based CDP is only available when *connecting to an already-running remote browser* (`connectOverCDP()`), a fundamentally different usage pattern pest-plugin-browser doesn't use. Switching transport would mean patching Playwright's own bundled internals, ruled out as out of scope (matching Session 17's own reasoning for not patching vendor code). Third, with that route closed, Session 18 made its one concrete attempt at a different, equally well-documented cause of exactly this symptom class — Docker's default 64MB `/dev/shm` (Playwright's own Docker guidance names this as a common cause of headless Chromium spawning a full process tree and then making no further progress): added `shm_size: "1gb"` to the `app-e2e` Compose service and re-ran the suite. **Result: unresolved**, confirmed by process-tree inspection (a full Chromium tree spawned, `--no-sandbox`/`--disable-dev-shm-usage` already applied automatically by Playwright, and a clean bounded CPU-tick measurement showing near-zero progress — the same signature as Session 17). The hung process tree was killed cleanly afterward. | Operability (test infrastructure) | Medium — blocks the one test suite that proves Success Metric #1's UI-only, zero-DevTools claim end-to-end via real DOM interaction; the backend contract those buttons depend on is independently re-confirmed against a live stack (Session 17's manual walkthrough), so the risk is narrower than "the buttons might not work," but the gap to "a real click was proven to fire them" is real and not closed | Confirmed to reproduce across two independent hosts with an identical signature across three sessions of investigation (Session 16 diagnosis, Session 17 disproof of the "sandbox-specific" hypothesis via a second host, Session 18 disproof of Session 17's own fd-inheritance mechanism theory via direct source inspection plus one targeted, well-documented fix attempt) | **Formally accepted as a residual risk, per explicit decision at Session 19 (2026-08-17) — not "in progress."** Three sessions of rigorous, evidence-based investigation is enough: a diagnosis, a disproof of that diagnosis by actually reading the vendor source rather than re-asserting it, and the two most commonly documented fixes for this exact symptom class both tried and both ruled out (one already applied by Playwright itself, the other not exposed by any public API for a local launch), each confirmed via a repeatable method (process-tree inspection + bounded CPU-tick measurement, not a one-off observation). Further session time will not be spent chasing the underlying Playwright/Docker hang unless something changes materially — e.g. a `pestphp/pest-plugin-browser` or Playwright version update that documents a fix for this failure class upstream, or a report that a Pest/Laravel-free minimal Playwright repro (the one lead Session 18 narrowed to but did not itself attempt) behaves differently. **Standing mitigation for backend/API confidence:** the curl-based manual-verification walkthrough against the real running docker-compose stack (first performed Session 17, re-confirmed via Session 16/18 handoffs) — a real, bracketed, end-to-end run of the DSAR lifecycle (login → verify-identity → approve-erasure → connector dispatch → deletion certificate) over real HTTP, substituting for automated browser-driven confirmation of the backend contract every admin-dashboard button calls. **Stated plainly, not glossed over: client-side Vue rendering of the admin dashboard (Sessions 14/15's `Login.vue`, `DsarQueue.vue`, etc.) remains genuinely unconfirmed by any automated means.** The curl-based substitute proves the backend contract those buttons call; it does not prove a real browser click fires them — that gap is accepted as residual, not closed, and would need either a working browser-driven E2E run or a stopwatch-timed human click-through (neither has happened) to close for real. | **Accepted (residual risk)** | Session 19 (2026-08-17) |

## Closed risks

| ID | Risk | Category | Impact | Likelihood | Resolution | Status | Closed |
|---|---|---|---|---|---|---|---|
| R-07 | Cold Docker image build time on a stranger's first `docker compose up --build` may independently exceed Success Metric #1's 15-minute budget. Session 17 confirmed this directly (2083s/~34.7 min measured on a real cold clone). | Operability | Was Medium (a slow or failed first build is exactly the kind of friction that loses a stranger evaluating this project) | Was Confirmed — a genuinely first-ever cold clone reliably exceeded the 15-minute budget | **Root cause found and fixed, then re-measured.** `docker/Dockerfile` was a single, undifferentiated build stage: the *same* image `docker-compose.yml`'s `app`/`worker` services build and run by default also unconditionally installed Node.js, npm, Playwright, and a real downloaded Chromium (`--with-deps`) — tooling only `pestphp/pest-plugin-browser`'s `tests/Browser/` suite ever uses, never application code at runtime. A stranger running the plain app was paying for browser-testing tooling they would never use. **Fix:** split into a multi-stage Dockerfile — `runtime` (default target, no Node/npm/Playwright/Chromium at all) and `test` (extends the same shared dependency layer with Node/Playwright/Chromium, used only by a new Compose-profile-gated `app-e2e` service: `docker compose --profile e2e run --rm app-e2e composer test:e2e`). **Re-measured with the identical rigour Session 17 used** (`docker compose down`, `docker rmi -f` on all project images, `docker builder prune -af`, confirmed 0B cache/0 images, then a single bracketed `docker compose up --build -d`), on the same class of host (Windows 11 + Docker Desktop/WSL2): **643 seconds (~10.7 minutes)** — a ~69% reduction, now under the 900-second budget on its own. The final `app` image also shrank from ~3.3GB to 1.32GB, consistent with removing that tooling. **One genuine regression found by actually running the tests, not assumed fixed:** the initial split moved `sockets`/`pcntl` (PHP extensions) into the `test` target only, reasoning they're only *used* by `tests/Browser/` — but `pestphp/pest-plugin-browser` hooks into Pest's global test lifecycle at boot regardless of which file runs, so **every** `composer test` invocation (not just the browser suite) failed with `Call to undefined function Pest\Browser\Support\socket_create_listen()` once those extensions were removed from `runtime`. Fixed by keeping `sockets`/`pcntl` in the shared base layer both targets build from — confirmed by directly timing the isolated compile step (**73 seconds**, materially more than this session's own Dockerfile comment first assumed before actually measuring it, corrected in place) and by re-running the full Feature suite (165/165 passing). **Honest gap, stated plainly:** a fresh, fully clean cold-clone timing of the *exact final* Dockerfile (extensions restored) could not be completed this session — repeated full builds in a short window tripped GitHub's anonymous `codeload.github.com` rate limit (a session-specific artifact of this session's own repeated testing, not a repository defect; a real single-attempt clone by an actual stranger would not reproduce this). The fix's *functional correctness* is nonetheless directly confirmed (165/165 tests passing against a live-patched container, sidestepping the rate limit entirely since no package re-download was needed for that check), and the extension-compile step's own real cost (73s) is directly measured, not guessed — so the best-supported estimate for the final Dockerfile's cold-clone time is **643s + 73s ≈ 716s (~11.9 min)**, still comfortably under the 900s budget, though this specific sum has not been confirmed by one single unbroken timed run. Session 16's two build-cache fixes (layer reordering, `worker`'s duplicate build elimination) remain in place and unaffected by this session's stage split. Not attempted: a registry-hosted prebuilt image (would remove the build entirely for a self-hoster, but needs a registry + CI publishing step, out of scope). **Session 19 (2026-08-17) follow-up check, explicit and dated, not a passive note:** re-tested the same rate limit before attempting the outstanding clean rebuild (`curl -sI https://codeload.github.com/laravel/framework/zip/refs/tags/v11.0.0`) — **still returns `HTTP/2 429`**, unchanged from Session 18, same calendar day; confirmed general (not endpoint-specific) against a second package URL. No rebuild was attempted against a still-blocked limit — would only waste session time re-tripping the same wall for the same result. **Follow-up trigger:** the next session that touches this repository, whether or not R-07 is its stated focus, should re-run that exact curl check once, cheaply, before other work. If it returns `200`/`302` (cleared), immediately run one clean `docker builder prune -af` + `docker compose up --build -d` cycle against the current `docker/Dockerfile` and record the real, atomic number here in place of the composed estimate. **If the check still returns `429` on or after 2026-08-24** (one week after Session 18's original trip — long enough that "still rate-limited" would no longer plausibly be the same transient event), treat it as no longer a simple transient limit: either retry from a different network/IP to isolate whether this host specifically is still flagged, or explicitly record a decision to accept the composed ~716s estimate as the permanent standing number rather than let "pending verification" silently carry forward indefinitely. **Post-tag verification check (2026-08-18):** re-ran the same `curl -sI https://codeload.github.com/laravel/framework/zip/refs/tags/v11.0.0` check per the follow-up trigger above — **now returns `HTTP/1.1 200 OK`**, cleared. Per the trigger's own instructions, this means the outstanding clean-rebuild re-verification is now actionable. **It has not yet been run** (a full `docker builder prune -af` + timed `docker compose up --build -d` cycle is non-trivial, ~11+ minutes, and was out of scope for a verification-only pass) — this is genuinely open, actionable follow-up work, not a passive note, and should be picked up before it's assumed the composed 716s estimate is final. **Genuine clean-rebuild re-verification, finally performed (2026-08-19):** the outstanding rebuild above was actually run — not a live-patched substitute. True-zero state was established for the dev stack specifically (`docker-compose.yml`'s project, `privacy-forge`; the separate `privacy-forge-prod` project from Deployment Session A was deliberately left running and untouched, being out of scope for this Dockerfile): `docker compose --profile e2e down -v --remove-orphans` (removed all dev containers, the `postgres-data`/`minio-data` volumes, and the project network), `docker rmi -f` on `privacy-forge-app`/`privacy-forge-frontend`/`privacy-forge-app-e2e`, then `docker builder prune -af` — confirmed via `docker builder du` showing **0B reclaimable** and via `docker images` showing none of this project's dev images present, before every attempt. **Three attempts failed first, and that is reported plainly rather than discarded:** attempt 1 ran ~3.8 hours before failing on `composer install` with DNS-resolution and TLS read failures reaching `codeload.github.com`/`api.github.com`; attempts 2 and 3 failed the same way after ~38 and ~37.7 minutes respectively, each time on a *different* package. Direct connectivity checks between attempts (host `curl`, and a fresh `alpine` container's `wget`) succeeded immediately each time, and attempt 4's own base-image pull was fast and unremarkable — pointing at transient host/network instability across that stretch of the session, not a defect in `docker/Dockerfile` (unchanged since Session 18) or in GitHub's service generally. **Attempt 4 succeeded cleanly, unbroken, from the same confirmed-zero state**: `docker compose build` completed in **423.436 seconds (~7.06 minutes)**, exit code 0, both `privacy-forge-app:latest` (1.32GB — matching Session 18's reported post-split size exactly) and `privacy-forge-frontend` built. Full verification followed against that exact image, not a substitute: `docker compose up -d` brought all six dev services up; `docker inspect --format '{{.RestartCount}}'` read **0** for every service; `docker compose ps` showed all healthchecked services `(healthy)`; `curl localhost:8000/up` returned **HTTP 200**; `docker compose exec app composer test` ran the full Feature suite inside that freshly-built, still-running container and reported **187/187 passing (758 assertions, 114.94s)** — the count is higher than Session 18's 165 because Sessions 19–25 added retention/RoPA/ABAC-UI/audit-log/retention-history tests since then, not because this run behaves differently. **Honest comparison, as requested:** 423s does **not** match the previously-reported 643s (Session 18) or the composed ~716s estimate — it is genuinely faster, by roughly 34% against 643s and 41% against 716s. The most defensible explanation is that build wall-clock time here is dominated by Packagist/GitHub download throughput and Docker Hub base-layer pull speed, both of which vary session-to-session with host/network conditions rather than with anything in the Dockerfile's own logic (unchanged since Session 18) — this session's own three failed attempts, on the very same unchanged Dockerfile, are direct evidence of exactly that variance, ranging from a multi-hour failure to a 7-minute clean success within a few hours of each other. Read across every measurement taken of this split Dockerfile to date (643s, the ~716s composed estimate, and now 423s), the honest range for a stranger's real first build is roughly **7–12 minutes**, comfortably under the 900s/15-minute budget in every case; the specific "643s" figure should not be treated as a fixed constant, but the qualitative finding it supported — the split fixed the budget-busting regression — holds up and is now confirmed by a real, unbroken, from-true-zero measurement rather than a composed estimate. Pre-split baseline (2083s/~34.7 min, Session 17) is unaffected by any of this and remains the correct reference point for "what an undifferentiated single-stage build cost." | **Closed** | Session 18 (2026-08-17); rate-limit re-check Session 19 (2026-08-17, still blocked); rate-limit re-check 2026-08-18 (cleared); **genuine clean-rebuild re-verification completed 2026-08-19 — 423s, 187/187 tests passing, real unbroken measurement, not composed or live-patched** — re-open only if a future genuinely-clean cold-clone measurement of the exact final Dockerfile comes back over the 900s budget |
| R-02 | No seeding/bootstrap mechanism existed for `PolicyDefinition` rows — a fresh instance had no active `dsar.identity.verify`, `dsar.erasure.approve`, `policy.update`, `retention.policy.manage`, or `ropa.export` policy until each was inserted manually, so ADR-0006's fail-closed evaluator denied every staff action by default. | Operability | Was Low (fails safe, not open — an availability/usability gap, not a security exposure) | Was High (true on every fresh install by default) | Added `database/seeders/PolicyDefinitionSeeder.php` (all five sensitive-action policies, exact shapes matching the ADRs/existing factory states) and `database/seeders/DatabaseSeeder.php` calling it — `php artisan db:seed` on a fresh instance now seeds all five in ~4 seconds. Idempotent (`firstOrCreate` keyed on `action_name`+`status`), so re-running it is a no-op rather than a duplicate-row error. Verified against a genuinely fresh database this session (`migrate:fresh` → `db:seed` → real admin verify-identity/approve-erasure over real HTTP — see `12-session-handoff.md`'s timing walkthrough) — no fail-closed denial anywhere in the path. README's step 0 rewritten to run this instead of the old `PolicyDefinition::create()` tinker calls. | **Closed** | Session 16 (2026-08-17) |
| R-03 | ADR-0006 commits to `policy.update` as a gated, audited sensitive action. Session 9's NFR-005 matrix found no controller/route implemented this — policies were only editable via CLI/seeders, a documented-but-unimplemented control rather than an active vulnerability. | Security | Was informational/low; would have become a real gap the moment an ungated policy-editing endpoint shipped | N/A (control gap, not an active exploit path) | Session 10 built `GET /admin/policies`, `GET /admin/policies/{id}`, and `PATCH /admin/policies/{id}` (`App\Http\Controllers\Admin\PolicyController`), all three gated by a real `policy.update` `PolicyEvaluator::evaluate()` call — Owner-only, audit-logged, fail-closed (both `policy_missing` and `evaluation_error` reason codes tested). Added to `AuthorisationMatrixTest.php`'s dataset (now 3 actions, 15 cells) and covered by a dedicated `tests/Feature/PolicyManagementTest.php`. All 110 tests pass, Pint clean, Larastan level 8 clean. | **Closed** | Session 10 (2026-08-15) |
| R-05 | No staff authentication (login/session) existed. Every sensitive action was reachable only via test harness (`actingAs()`) or shell access (`php artisan tinker`), never a real HTTP session — blocking genuine use of the product and leaving T-11/T-12/T-13 untested against anything real. Highest-priority open item at the time it was added. | Security / Operability | Was High (nobody could actually use the product as a real staff user) | Was Certain (true on every install by construction) | Built real session-based login: `App\Http\Controllers\Auth\AuthenticatedSessionController` (`GET/POST /login`, `POST /logout`), a Vue `Login.vue` page matching the existing house style, and `php artisan privacy-forge:create-owner` to bootstrap the first Owner account without shell access to application code. Session regenerates on login and is fully invalidated (guard cleared, session invalidated, token rotated) on logout (T-11) — `tests/Feature/LoginRateLimitTest.php`. Rate-limiting is hand-rolled against the cache rather than `RateLimiter::hit()`, whose decay argument only takes effect on a key's first call — each further failed attempt at least doubles the lockout window (capped at 5 minutes), and unknown-email/wrong-password produce the identical generic message (T-13) — same test file. CSRF (T-12) was verified with a real test that defeats Laravel's `runningUnitTests()` CSRF bypass (which silently exempts every ordinary Pest test, including every pre-existing `actingAs()`-based test in this suite) — `tests/Feature/CsrfProtectionTest.php` proves a state-changing admin request is rejected (419) without a token and succeeds with one. `tests/Browser/DsarLifecycleTest.php`'s admin steps now authenticate via real `POST /login` calls (session cookie captured and forwarded by hand, since Laravel's test client doesn't do this automatically) instead of `actingAs()`; a new `tests/Browser/StaffLoginUiTest.php` drives the actual Login.vue form in a real Playwright browser. **Correction to this session's own brief:** `App\Models\User` (with its `role` enum, factory, and `isPrivilegedFor()` helper) and its migration already existed prior to this session — confirmed the role representation (`owner`/`privacy_manager`/`support_staff` lowercase strings) already matches exactly what `PolicyEvaluator` and all five registered policies expect, so no changes were needed there; only the login/logout/bootstrap layer around it was missing. | **Closed** | Session 14 (2026-08-18) |
| R-06 | No connector in v1 had a real, working webhook receiver — ADR-0004 deliberately scopes connectors as external, third-party-operated services, so nothing in the repository listened on any `webhook_url`; every existing test (`ConnectorDispatchTest.php`, `ConnectorCallbackAuthTest.php`) either `Http::fake()`d the outbound leg or hand-signed a `postJson()` call simulating a connector that had never actually been built. **Re-scoped this session, per explicit instruction to verify rather than assume**: reading every connector test file confirmed this was not merely "the README's demo connector points at a placeholder" (the prior session's framing) but that **no real, standalone connector — reference/stub or otherwise — had ever been exercised over real HTTP in this project's history**; `RegisterReferenceConnectorCommand` (which existed) only ever created a database row, and its default `webhook_url` (`{app.url}/reference-connector/webhook`) pointed at a route that had never been built. This made R-06 a materially bigger finding than its original description: the DSAR completion path had never been proven to work over real HTTP, only against in-process test doubles. | Operability | Was High (true on every fresh self-hosted install that follows the README exactly, by construction) — the delay/wrong-status-word was not a security exposure, but "no connector has ever really worked" was a real gap in what had actually been proven | Was High | Built `App\Http\Controllers\ReferenceConnectorWebhookController` — a real, minimal reference/stub connector living in this same application (a deliberate choice: it still requires two genuinely separate real HTTP hops, and in the docker-compose demo those hops cross real container boundaries — `worker` dispatches to `app` — without the deployment complexity of a wholly separate service; see `12-session-handoff.md` for the full reasoning). It independently reimplements the HMAC contract by hand (not reusing `ConnectorSignatureService`), receives the real signed webhook, and fires its own real, independently-signed callback to `/api/v1/connector-callback/{taskId}`. `RegisterReferenceConnectorCommand`'s default `webhook_url` now points at this real route via a new `connectors.reference_connector_base_url` config value (`REFERENCE_CONNECTOR_BASE_URL`, defaulting to `http://app:8000` — deliberately not `config('app.url')`, which resolves to the calling container itself, not `app`, breaking cross-container delivery from `worker`). Proven two independent ways: (1) `tests/Feature/ReferenceConnectorWebhookTest.php`, a real Feature test chaining the actual `DispatchConnectorTaskJob` → `ReferenceConnectorWebhookController` → `ConnectorCallbackController` — three real production classes — via one HTTP-shaped hop each, reaching `status: complete` on a fresh seed (not `partially_complete`); (2) a genuine manual walkthrough this session against the real running docker-compose stack (`migrate:fresh` → `db:seed` → `connectors:register-reference` → real browser-equivalent curl calls for verify-identity/approve-erasure → polling), where the erasure DSAR reached `complete` with a deletion certificate in ~46 real seconds via the actual `worker` container dispatching a real signed webhook across the Docker network to the actual `app` container, which called back for real — see `12-session-handoff.md`. One honest, real observation from that walkthrough: the first webhook attempt timed out after 30s (`php artisan serve`'s single-threaded dev server can't accept the reference connector's own outbound callback while still handling the inbound webhook request that triggered it) — the existing ADR-0004 retry/backoff correctly handled this and the retry succeeded in 106ms; not a new tracked risk, since it's a known, already-mitigated dev-server characteristic, not a gap in the contract. README step 4 rewritten to describe `complete`, not `partially_complete`, as the expected outcome. | **Closed** | Session 16 (2026-08-17) |
| R-01 | Audit log lacked DB-level grant revocation (ADR-0003) — the app's Postgres role owned `audit_log_entries`, so `UPDATE`/`DELETE` privileges could not be revoked from it; the hash chain (R-04's anchoring aside) was the only real protection against the app's own runtime DB credential tampering with an entry. | Security | Was Medium | Was Low (requires the app's own runtime DB credential to be used maliciously or buggily, not a full DB compromise) | Created a second, genuinely non-owning Postgres role, `privacy_forge_app` — `SELECT`/`INSERT` only on `audit_log_entries`, full DML elsewhere — that the running application now connects as for everything; the schema-owning role (`privacy_forge`) is used only for `php artisan migrate --database=pgsql_migrate` (`config/database.php`'s new `pgsql_migrate` connection, `database/migrations/2026_08_19_000001_add_restricted_runtime_role_for_audit_log.php`). **The originally-assumed alternative — the owning role revoking its own `UPDATE`/`DELETE` — was tested directly against a real Postgres instance and found to work only as a soft barrier**: Postgres does let an owner revoke its own privileges, but the same role can just as trivially `GRANT` them back to itself afterward (ownership carries the right to alter a table's ACL regardless of the ACL's own contents), so a self-revoke doesn't hold against the actual threat (the app's own credential running attacker-controlled or buggy arbitrary SQL) — full reasoning in `09-decision-log.md`'s Session 27 entry. Surfaced a genuine correctness fix along the way: Postgres requires the `UPDATE` privilege for `SELECT ... FOR UPDATE`/`FOR SHARE` even without an actual `UPDATE`, so `AuditLogger::record()`'s row-locking was replaced with `pg_advisory_xact_lock()`, which needs no table privilege at all. **Proven, not just designed:** `tests/Feature/AuditLogGrantEnforcementTest.php` connects as the real app runtime role and issues raw SQL `UPDATE`/`DELETE` against `audit_log_entries` (not through the model's already-existing application-layer guard), both rejected with Postgres error `42501`; `SELECT`/`INSERT` still work. Independently reproduced via a raw `psql` session against both `docker-compose.yml` and `docker-compose.prod.yml`'s Postgres, and end to end against the running prod-shape stack: the role-creation migration run against its *existing* data volume (idempotent, not just fresh-install-only), a real `privacy-forge:create-owner`, a real `POST /login` over HTTPS, and a real authenticated `GET /api/v1/admin/audit-log` returning that login's own audit entries. 191/191 tests pass (185 Feature + 6 Unit; 187 previously reported for the Feature suite alone at v1.0.0, plus 4 new tests here), Pint clean, Larastan level 8 clean, ESLint clean, OpenAPI unchanged. **Session 28 follow-up, two gaps this Closed status had not yet actually verified, checked before letting it stand:** (1) **Concurrency of the advisory lock.** `CHAIN_LOCK_KEY_SEED` (`app/Services/AuditLogger.php`) is a single, fixed, global string (`'privacy_forge:audit_log_chain'`) hashed by `hashtext()` into one `pg_advisory_xact_lock` key — not partitioned by actor, resource, or anything else, so every write genuinely contends for the same lock; there was no test that actually fired concurrent writes to prove it holds, only sequential ones. Added `tests/Feature/AuditLogConcurrencyTest.php`, which forks 8 real OS child processes (`pcntl_fork`, each opening its own independent Postgres connection) that call `AuditLogger::record()` at genuinely the same time, then asserts the resulting chain is gapless and unforked (each entry's `prev_hash` matches the entry immediately before it by sequence) and that `verifyChain()` still passes end to end. Proven both ways required: it passes with the lock in place, and — checked directly, not assumed — reliably **fails** (reporting a forked `prev_hash`, one entry pointing at the genesis hash instead of its real predecessor) when the lock line is temporarily commented out and the race window widened with a `usleep`, confirming the test actually has teeth before trusting it. Writing this test surfaced a real pitfall worth recording: a naive `pcntl_fork` + shared PDO connection reliably corrupted the *parent's* connection ("server closed the connection unexpectedly") because Laravel's `disconnect()`/`purge()` (and even a plain child `exit()`, via Zend's normal object-destruction sweep) triggers a real libpq `PQfinish()` on what is, at the kernel level, the *same* duplicated TCP connection the parent still holds — the fix was to leave the inherited connection completely untouched in each child, write through a separately-named fresh connection instead, and terminate each child via `posix_kill(SIGKILL)` rather than `exit()`, since only a raw signal skips PHP's own destructor sweep. CI's `php-quality` job now installs `pcntl`/`posix` for this. (2) **Migrate-role credential scoping.** Checked precisely, not assumed: before this session, `docker-compose.prod.yml`'s `app` and `worker` both declared `env_file: .env`, and `.env` held `DB_MIGRATE_USERNAME`/`PASSWORD` — confirmed via `docker exec privacy-forge-prod-app-1 printenv` (and the same for `worker`) that the schema-owning role's real credentials were sitting in both long-running services' actual process environment, reachable by a compromised app/worker process with zero extra effort, entirely defeating R-01's point regardless of whether normal request handling ever used them. The dev stack had the same exposure through a different path: `docker-compose.yml` bind-mounts the whole repo, so `.env` (with those same credentials) sat readable on disk inside `app`/`worker`, and Laravel's own dotenv loading made `env('DB_MIGRATE_USERNAME')` return the real value from any code path in that process, confirmed via `php artisan tinker`. **Fixed** by splitting the owning role's credentials into a new `.env.migrate` (gitignored, `.env.migrate.example` committed) that is loaded by neither `app` nor `worker` in either compose file, and introducing a one-shot, profile-gated `migrate` Compose service (`docker compose --profile migrate run --rm migrate php artisan migrate --database=pgsql_migrate` / `... demo:reset` / `... composer test`, replacing `docker compose exec app ...` for all three) that is the only place `.env.migrate` is ever loaded. `demo:reset`'s `pgsql_migrate` TRUNCATE (`ResetDemoInstanceCommand`, a real, pre-existing, DEMO_MODE-gated use of the owning role at runtime — not something this session introduced) now runs the same way. Re-verified concretely after the fix: `docker exec` on both the recreated `privacy-forge-prod-app-1` and `-worker-1` containers shows no `DB_MIGRATE_*` in their real environment; the dev `app` container's own bind-mounted `.env` file on disk no longer contains those credentials either; the one-shot `migrate` service was run for real against the live prod stack (`php artisan migrate --database=pgsql_migrate --force`, correctly reported "Nothing to migrate" against the already-migrated schema) and, separately, against a fresh throwaway stack with its own isolated volumes (to avoid destructively truncating the real demo/dev data) where both `migrate` and `demo:reset` were run for real and succeeded, then the throwaway stack was torn down. One real regression this restructuring caused and fixed before trusting it: routing `composer test` through the new one-shot service via `env_file: .env` initially broke CSRF handling in 67 tests (419 instead of 2xx) — Docker's `env_file` sets real OS environment variables before PHP starts, and dotenv's default immutable mode won't override an already-set real var, so `.env`'s `APP_ENV=local` silently beat phpunit.xml.dist's `APP_ENV=testing` override, disabling the test-only CSRF bypass. Fixed by having the dev `migrate` service load only `.env.migrate` via `env_file` (matching `app`/`worker`, which already relied on Laravel's own dotenv reading the bind-mounted `.env` rather than Docker-level injection) — confirmed by re-running the full suite clean (192/192, including the new concurrency test) immediately after. | **Closed** | Session 27 (2026-08-19); concurrency test and migrate-credential isolation added and verified Session 28 (2026-08-19) |
| R-04 | ADR-0003's external chain-anchoring for the audit log had never been implemented — only the hash-chain (entry-level tamper detection) and DB-grant (R-01) layers existed. A known, designed-for degradation (ADR-0003 explicitly anticipated "chain-only" as an accepted, weaker state), not an unexpected gap — but a real one until closed. | Security | Was Medium (the stronger guarantee ADR-0003 describes — protection against an attacker who edits an entry *and* recomputes the chain — was not available; entry-level detection alone still held) | Was Low today, rising over time as the product is deployed and relied on as evidence | Built `AuditLogger::anchorChain()`/`verifyAnchors()` and two commands: `audit:anchor-chain` (scheduled hourly, `routes/console.php`) and `audit:verify-chain` (the runbook item ADR-0003's Consequences section explicitly calls for). Anchors are written to the existing `s3` disk (the same external object storage export bundles already use — no new infrastructure dependency), one immutable-by-construction object per anchored sequence (`audit-anchors/{sequence}.json`), never overwritten — deliberately *not* stored in this application's own Postgres database, since a DB-stored anchor would be exactly as editable by the attacker ADR-0003's threat model assumes as the entries themselves, defeating the point. **Proven with real evidence, not just an assertion the code runs** (`tests/Feature/AuditChainAnchorTest.php`): the key test builds a real 3-entry chain, anchors it, then simulates a genuinely privileged DB-access attacker — bypassing `AuditLogEntry`'s append-only guard via `DB::table()->update()` (the same technique `ConsentCaptureTest.php` already uses) — tampering with the *first* entry and correctly recomputing every subsequent `prev_hash`/`entry_hash` so the chain is internally consistent again. `verifyChain()` alone is fooled (`valid: true`) — proving the exact gap ADR-0003 describes. `verifyAnchors()` is not: it detects the mismatch at the anchored sequence, because the anchor written *before* the tampering still holds the *original* hash. Additional tests cover `anchorChain()`'s `no_entries` case, idempotency (re-anchoring an unchanged chain writes identical content, not a duplicate), and the command's alerting behaviour (`AnchorAuditChainCommand` logs `Log::critical` and exits non-zero on a failed external write — a Storage stub forced to fail, per ADR-0003's Consequences requirement that anchor unavailability "must trigger an alert, not fail silently"). 165/165 Feature tests pass (160 carried over + 5 new), Pint clean, Larastan level 8 clean, ESLint clean, OpenAPI unchanged (CLI-only, no new HTTP surface). `01-scope-and-non-goals.md`'s MVP checklist item for this is now checked. | **Closed** | Session 17 (2026-08-17) |

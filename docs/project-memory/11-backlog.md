# Backlog
> Purpose: everything deliberately not being done right now
> Project: privacy-forge (public)
> Last updated: 2026-08-19 (staleness audit — B-04/B-05/B-06 were already closed at Sessions 22–24 but this file and the v1.0.0 release notes still listed them as open; moved to Closed below, count corrected to 3 genuinely open)

## Next up
| ID | Item | Type | Size | Why now |
|---|---|---|---|---|
| B-01 | Full-instance archival export (`php artisan privacy-forge:export-instance` or similar) — one JSON+CSV export covering every consent record, DSAR, retention execution, deletion certificate, and the audit log at once, reusing `ExportBundleAssembler`/`RopaController`'s existing JSON/CSV shaping conventions rather than a new format. Identified as a genuine gap at Session 19 (`14-maintenance-and-retirement.md`, "Archival export format") — today's only available substitute is a raw `pg_dump`, which is not guaranteed to `pg_restore` cleanly across future PostgreSQL major versions. | Feature | Medium | Closes the one real gap in the instance-decommissioning runbook; not built at Session 19 per that session's explicit scope boundary (a new, non-trivial feature, not a "trivial missing piece") |
| B-02 | `RetentionPolicyController::store` does not prevent two independently-created `active` policies for the same `data_category_id` (only the `update` path's supersede-then-create flow guarantees uniqueness) — a Session 11 gap, restated at Session 12 and again at Session 19 (`14-maintenance-and-retirement.md`), previously undertracked here. Re-confirmed still open 2026-08-19: `store()` (`app/Http/Controllers/Admin/RetentionPolicyController.php`) still creates a new `active` policy with no check for an existing active row on the same `data_category_id`. | Bug | Small | A real, if narrow, data-integrity gap in the retention engine; low urgency (requires two independent create calls racing, not a normal UI flow) but cheap to fix once picked up |
| B-03 | Weekly `schedule:`-triggered re-run of the existing `osv-scanner` CI job — currently only runs `on: push`/`pull_request` to `main` (`.github/workflows/ci.yml`), so a CVE published against an already-merged dependency with no further pushes is never re-caught. Identified at Session 19 (`14-maintenance-and-retirement.md`, "Maintenance cadence"). Re-confirmed still open 2026-08-19: `.github/workflows/ci.yml` has no `schedule:` trigger. | Chore | Small | Cheap (reuses the existing job, just adds a trigger); not added at Session 19 to avoid touching CI config under a documentation-focused session |
## Later
## Explicitly rejected (with reasons)
## Closed (kept for history)
| ID | Item | Resolution |
|---|---|---|
| B-04 | `GET /admin/audit-log` was fully documented in `docs/architecture/openapi.yaml` (the "Admin -- RoPA and Audit" tag) but had no implementation at all. | **Closed, Session 22** (`c23f665`). `AuditLogController::index`, routed at `GET /admin/audit-log` (`routes/api.php`), role-scoped full-vs-own-actions visibility, wired into `AdminAuditLog.vue`, covered by Feature tests. Confirmed still present in the codebase as of 2026-08-19's staleness audit — this file and the v1.0.0 release notes had continued to list it as open through Session 25's tag, which was inaccurate. |
| B-05 | No read endpoint existed to list past `RetentionExecution` records with their linked `DeletionCertificate`. | **Closed, Session 22** (`c23f665`). `RetentionPolicyController::executions`, routed at `GET /admin/retention-policies/{policyId}/executions` (`routes/api.php`), via `RetentionExecutionResource`, wired into `AdminRetention.vue`. Confirmed still present in the codebase as of 2026-08-19's staleness audit — this file and the v1.0.0 release notes had continued to list it as open through Session 25's tag, which was inaccurate. |
| B-06 | No production-grade application image existed. | **Closed, Sessions 23–24** (`5d0ac8e`, then Session 24). The image half closed at Session 23: `docker/Dockerfile.prod` (`app` = PHP-FPM + OPcache, `web` = Caddy) and `docker-compose.prod.yml`, verified over real HTTP locally. The infra-provisioning half was descoped by explicit decision at Session 24 (`09-decision-log.md`), not left open. This file already carried a "Fully closed, Session 24" note on this row but had mistakenly left it in the "Next up" table instead of here — corrected 2026-08-19. |
| B-07 | The public demo instance's synthetic seed data had no design beyond the minimal ABAC-policy bootstrap (`PolicyDefinitionSeeder`, R-02) and the reference connector. | **Resolved by decision, Session 24, not built out.** Session 24 descoped ever running a live public instance (`09-decision-log.md`) — the thing B-07 was for (a *compelling* experience for a real visitor browsing a real public URL) no longer has a live audience to be compelling to. The existing minimal baseline (`demo:reset`'s current output: five ABAC policies + the reference connector) is decided as sufficient for this portfolio build's actual remaining purpose — proving the deployment/safety mechanics work — and richer sample content is downgraded from "needed before go-live" to ordinary optional polish, not reopened as a blocking item. If this project is ever actually deployed live for real, B-07's original reasoning (richer content matters for a real visitor) applies again and should be re-filed. |
| B-08 | No scoped, temporary per-visitor demo identity mechanism existed (Demo Instance Data Safety control 2, `06-security-threat-model.md`) — the only way to get a staff session was `php artisan privacy-forge:create-owner`, a real, persistent credential; `ResetDemoInstanceCommand` (Session 22) deliberately left `users` untouched at each reset for exactly this reason. | **Closed, Session 24.** `users` is now included in `demo:reset`'s truncate list and the command re-creates exactly one fixed, documented demo-viewer account (`config('demo.viewer_email')`/`viewer_password`) every time it runs — never a stale credential, never a locked-out visitor. Verified end-to-end: real login over HTTPS with these exact credentials, then a real authenticated API call. This is a deliberate simplification of the original per-visitor-scoped-identity design goal, acceptable specifically because Session 24 also descoped ever exposing this instance to real public traffic (a fixed credential is only a real problem — T-19 — when there's a public internet full of strangers who could find and abuse it). **Must be revisited before any future real public deployment** — see `06-security-threat-model.md`'s T-19 row and Demo Instance Data Safety control 2. |

# Backlog
> Purpose: everything deliberately not being done right now
> Project: privacy-forge (public)
> Last updated: 2026-08-18 (Session 22)

## Next up
| ID | Item | Type | Size | Why now |
|---|---|---|---|---|
| B-01 | Full-instance archival export (`php artisan privacy-forge:export-instance` or similar) — one JSON+CSV export covering every consent record, DSAR, retention execution, deletion certificate, and the audit log at once, reusing `ExportBundleAssembler`/`RopaController`'s existing JSON/CSV shaping conventions rather than a new format. Identified as a genuine gap at Session 19 (`14-maintenance-and-retirement.md`, "Archival export format") — today's only available substitute is a raw `pg_dump`, which is not guaranteed to `pg_restore` cleanly across future PostgreSQL major versions. | Feature | Medium | Closes the one real gap in the instance-decommissioning runbook; not built at Session 19 per that session's explicit scope boundary (a new, non-trivial feature, not a "trivial missing piece") |
| B-02 | `RetentionPolicyController::store` does not prevent two independently-created `active` policies for the same `data_category_id` (only the `update` path's supersede-then-create flow guarantees uniqueness) — a Session 11 gap, restated at Session 12 and again at Session 19 (`14-maintenance-and-retirement.md`), previously undertracked here. | Bug | Small | A real, if narrow, data-integrity gap in the retention engine; low urgency (requires two independent create calls racing, not a normal UI flow) but cheap to fix once picked up |
| B-03 | Weekly `schedule:`-triggered re-run of the existing `osv-scanner` CI job — currently only runs `on: push`/`pull_request` to `main` (`.github/workflows/ci.yml`), so a CVE published against an already-merged dependency with no further pushes is never re-caught. Identified at Session 19 (`14-maintenance-and-retirement.md`, "Maintenance cadence"). | Chore | Small | Cheap (reuses the existing job, just adds a trigger); not added at Session 19 to avoid touching CI config under a documentation-focused session |
| B-04 | `GET /admin/audit-log` is fully documented in `docs/architecture/openapi.yaml` (the "Admin -- RoPA and Audit" tag) but has no implementation at all -- no route in `routes/api.php`, no controller under `app/Http/Controllers`. Found this session (21) while scoping the audit-log query view stretch item: `openapi-spec-validator` only checks the spec's own internal shape, so a documented-but-unbuilt path passes validation silently, and no Feature test exercises it either. A pre-existing spec/implementation gap, not a session-21 regression. | Bug (spec/impl drift) | Small-Medium | Blocks a future admin audit-log UI (this session's deferred stretch item 4); a check similar in spirit to the new `dependency-governance` CI job -- a documented path with zero implementation should be caught automatically |
| B-05 | No read endpoint exists to list past `RetentionExecution` records (dry-run or real) with their linked `DeletionCertificate` -- the only certificate-shaped data exposed over HTTP is `DsarStatus.deletion_certificate`, scoped to one DSAR's own erasure, not retention-driven executions. Found this session (21) building the retention policy management UI: mandatory scope item 1 asked for "past execution history... and their deletion certificates," and rather than fake this with placeholder data or silently extend the API contract, the UI states the gap explicitly and this item was filed instead. | Feature | Medium | Needed to fully satisfy the retention UI's original brief; pairs naturally with B-04 |
| B-06 | No production-grade application image exists. `docker/Dockerfile`'s own header comment and a `09-decision-log.md` Session 13 entry both refer to "the production reference deployment (PHP-FPM + a real web server, built at Session 8)" as an existing, separate artifact -- it does not exist. `docker/` contains only the dev/CI `Dockerfile` (`runtime`/`test` targets, both `CMD php artisan serve` -- a single-threaded dev server, not production-appropriate) and `Dockerfile.frontend` (the Vite dev server). Found Session 22 while scoping what needs to exist before the public demo can go live on a real URL. | Bug (doc/impl drift) + Feature | Medium | Hard blocker for the demo deployment session -- see `12-session-handoff.md`'s Part B plan; the same "documented but never built" shape as B-04 |
| B-07 | The public demo instance's synthetic seed data has no design beyond the minimal ABAC-policy bootstrap (`PolicyDefinitionSeeder`, R-02) and the reference connector -- no sample consent purposes/notices/records for a visitor to actually explore. `App\Console\Commands\ResetDemoInstanceCommand` (built Session 22) resets to this same minimal baseline, deliberately not inventing demo content without a real content design pass. | Feature (content design) | Medium | Needed for the demo to be *compelling*, not just safe; a content/product decision, not groundwork -- see `12-session-handoff.md`'s Part B plan |
| B-08 | No scoped, temporary per-visitor demo identity mechanism exists (Demo Instance Data Safety control 2, `06-security-threat-model.md`) -- today the only way to get a staff session at all is `php artisan privacy-forge:create-owner`, a real, persistent credential. `ResetDemoInstanceCommand` (Session 22) deliberately leaves `users` untouched at each reset for exactly this reason: truncating it today would lock out every visitor with nothing to replace it. | Feature (security design) | Medium-Large | A real open design question (how does a demo visitor get in without a real login, and without a long-lived shared credential) -- needs its own design pass, not a rushed implementation; see `12-session-handoff.md`'s Part B plan |
## Later
## Explicitly rejected (with reasons)
